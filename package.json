{
  "name": "surpriseme-extension",
  "version": "1.0.0",
  "private": true,
  "description": "SurpriseMe browser extension build + release tooling",
  "scripts": {
    "package": "node scripts/build-extension.js"
  },
  "dependencies": {
    "archiver": "^5.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}
